import styled from 'styled-components';

export const StyledText = styled.div`
    display:flex;
    align-items:center;
    place-items:center;
    justify-content: space-between;
    padding: 2rem 23rem;
`

export const StyledStars = styled.i`
    cursor:pointer;
    transition: all .5s linear;
`